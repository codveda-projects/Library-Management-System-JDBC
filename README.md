# Library-Management-System-JDBC
a Library Management System using Java and JDBC for database connectivity. The system should allow adding, borrowing, and returning books.

# compile and run
javac -cp ".;mysql-connector-java-5.1.42-bin.jar" *.java 

java -cp ".;mysql-connector-java-5.1.42-bin.jar" Main



